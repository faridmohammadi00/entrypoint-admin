import React from 'react';
import { Box, Typography } from '@mui/material';

const UserDetails: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">User Details</Typography>
      {/* Add details for a single user here */}
    </Box>
  );
};

export default UserDetails;
